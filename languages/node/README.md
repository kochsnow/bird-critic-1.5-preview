# Node.js track

Owned files:

- `index.html` — Node.js-specific page content
- `tasks.json` — rolling Node.js task catalog
- `results.json` — Node.js leaderboard results

Use `"language": "Node.js"` in every task and store scores under `scores.node`.

Shared styles and scripts are maintained centrally in `/assets`.
