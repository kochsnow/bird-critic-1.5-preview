# PHP track

Owned files:

- `index.html` — PHP-specific page content
- `tasks.json` — rolling PHP task catalog
- `results.json` — PHP leaderboard results

Use `"language": "PHP"` in every task and store scores under `scores.php`.

Shared styles and scripts are maintained centrally in `/assets`.
