# Python track

Owned files:

- `index.html` — Python-specific page content
- `tasks.json` — rolling Python task catalog
- `results.json` — Python leaderboard results

Use `"language": "Python"` in every task and store scores under `scores.python`.

Shared styles and scripts are maintained centrally in `/assets`.
