# Ruby track

Owned files:

- `index.html` — Ruby-specific page content
- `tasks.json` — rolling Ruby task catalog
- `results.json` — Ruby leaderboard results

Use `"language": "Ruby"` in every task and store scores under `scores.ruby`.

Shared styles and scripts are maintained centrally in `/assets`.
